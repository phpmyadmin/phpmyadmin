<input id="field_{{ column_number }}_{{ ci - ci_offset }}"
    type="text"
    name="field_comments[{{ column_number }}]"
    size="12"
    maxlength="{{ max_length }}"
    value="{{ value }}"
    class="textfield" />
