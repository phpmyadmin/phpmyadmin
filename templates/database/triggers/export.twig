<fieldset class="pma-fieldset">
  <legend>{{ 'Export of trigger %s'|trans|format(backquote(item_name)) }}</legend>
  <textarea cols="40" rows="15" class="w-100">{{ data|trim }}</textarea>
</fieldset>
