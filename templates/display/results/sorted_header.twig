<th class="draggable column_heading sticky
  {{- is_column_numeric ? ' text-end' }}{{ is_browse_pointer_enabled ? ' pointer' }}{{ is_browse_marker_enabled ? ' marker' -}}
  {{- is_column_hidden ? ' hide' }}" data-column="{{ column_name }}">
  {{ order_link|raw }}
  {{ comments|raw }}
</th>
