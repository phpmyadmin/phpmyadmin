{% if cfg_relation['relwork'] or is_foreign_key_supported %}
  <ul id="topmenu2">
    <li{{ route == '/table/structure' ? ' class="active"' }}>
      <a href="{{ url('/table/structure', url_params) }}" id="table_structure_id" class="tab{{ route == '/table/structure' ? 'active' }}">
        {{ get_icon('b_props', 'Table structure'|trans) }}
      </a>
    </li>

    <li{{ route == '/table/relation' ? ' class="active"' }}>
      <a href="{{ url('/table/relation', url_params) }}" id="table_relation_id" class="tab{{ route == '/table/relation' ? 'active' }}">
        {{ get_icon('b_relations', 'Relation view'|trans) }}
      </a>
    </li>
  </ul>
  <div class="clearfloat"></div>
{% endif %}

<div id="structure_content">
  {% block content %}{% endblock %}
</div>
